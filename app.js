const app=document.getElementById('app');
const dayCards=ITINERARY.map((x,i)=>`<article class="day" id="day-${i+1}"><button onclick="this.parentElement.classList.toggle('open')"><div class="date">${x.d}</div><div><div class="place">${x.p}</div><div class="summary">${x.s}</div></div><div class="chev">⌄</div></button><div class="detail"><ul>${x.a.map(a=>`<li>${a}</li>`).join('')}</ul></div></article>`).join('');
const route=ROUTE.map((x,i)=>`${i?'<span class="arrow">→</span>':''}<span class="stop">${x}</span>`).join('');
const checks=BOOKINGS.map((x,i)=>`<label><input type="checkbox" data-k="b${i}"><span>${x}</span></label>`).join('');
app.innerHTML=`<div class="wrap"><header class="hero"><h1>Severní Vietnam</h1><div class="sub">19. 9. – 6. 10. 2026 · interaktivní itinerář</div></header><section class="map" id="map"><h2 class="section-title">Trasa</h2><div class="route">${route}</div></section><section id="itinerary"><h2 class="section-title">Itinerář</h2>${dayCards}</section><section id="bookings"><h2 class="section-title">Rezervace</h2><div class="check">${checks}</div></section><nav class="nav"><a href="#map">Trasa</a><a href="#itinerary">Dny</a><a href="#bookings">Rezervace</a></nav></div>`;
document.querySelectorAll('input[data-k]').forEach(el=>{el.checked=localStorage.getItem(el.dataset.k)==='1';el.addEventListener('change',()=>localStorage.setItem(el.dataset.k,el.checked?'1':'0'));});
